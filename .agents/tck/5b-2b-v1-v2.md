# TCK: 5b-2b (fallback sends only recovered tool calls on v2 replay)

Scoped run per `.agents/tck/HOW-TO-RUN.md` (bun missing locally, built with `npm run build`).

## v1 (`-k test_session`)

18 passed / 0 failed / 2 skipped (SKIPPED: AUTH-A1 empty-authMethods case not applicable;
ACP-CLOSE-002 close-cancellation not exercised). `VERDICT: NOT CONFORMANT` is expected for a
scoped run (deselected requirements count as NOT TESTED); ignore it per the how-to.

## v2 (`--protocol-version 2 -k "test_session or test_resume"`)

24 passed / 0 failed / 2 skipped (SKIPPED: AUTH-205 empty-authMethods case; DELETE-202 session/list
never reported the session). ACP-RESUME-201..205 all PASS.

Both match the 5b-2a baseline (v1 18/0/2, v2 24/0/2) — no regression from the fallback filtering
change.
