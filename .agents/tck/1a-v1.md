# TCK v1: after slice 1a (dual-version dispatch + `initializeV2`)

- Date: 2026-09-24
- codex-acp: commit `852f102` (connection routed through `agentProtocolRouter().withV1(...).withV2(...)`),
  `dist/index.js` rebuilt with `npm run build`
- acp-tck: `5418887`, protocol version 1
- Command: see `HOW-TO-RUN.md`, "Full v1 suite"

## Result: VERDICT: CONFORMANT (exit 0)

56 tests: 50 passed, 1 failed, 5 skipped (200 s).

| Tier          | PASS | FAIL | SKIPPED | NOT_TESTED |
|---------------|------|------|---------|------------|
| MANDATORY     | 21   | 0    | 0       | 0          |
| CAPABILITY    | 15   | 0    | 4       | 0          |
| ADVISORY      | 10   | 1    | 1       | 0          |
| INFORMATIONAL | 4    | 0    | 0       | 0          |

## Failing requirements

- ACP-SCHEMA-002 (ADVISORY): pre-existing, unchanged (`session/new` returns `models`, `session/prompt` returns
  `usage`).

## Comparison with `v1-post-sdk-bump.md`

No regressions. Same single failure (ACP-SCHEMA-002) and the same five skips (ACP-AUTH-003, ACP-AUTH-004,
ACP-AUTH-005, ACP-PROMPTCAP-002, ACP-CLOSE-002). ACP-INFO-INVALIDREQ-001 and ACP-INFO-PARSE-001 still pass, so the
router's handling of the first wire message did not change what v1 clients observe.
